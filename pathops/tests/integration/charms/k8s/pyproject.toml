[project]
name = "test"
version = "0.1.0"
description = "Test charm for integration tests"
requires-python = ">=3.10"
dependencies = [
    "ops==3.*",
    "charmlibs-pathops",
]

[tool.uv.sources]
charmlibs-pathops = { path = "pathops" }
