# k8s.yaml
# automatically appended to charmcraft.yaml by the pack script

containers:
  workload:
    resource: workload

resources:
  workload:
    type: oci-image
    description: OCI image for the 'workload' container.
    upstream-source: some-repo/some-image:some-tag
