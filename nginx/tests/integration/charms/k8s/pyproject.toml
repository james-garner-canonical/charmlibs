[project]
name = "test"
version = "0.1.0"
description = "Test charm for integration tests"
requires-python = ">=3.10"
dependencies = [
    "ops==2.*",
    "charmlibs-nginx",
]

[tool.uv.sources]
charmlibs-nginx = { path = "nginx" }
