[project]
name = "test"
version = "0.1.0"
description = "Test charm for integration tests"
requires-python = ">=3.8"
dependencies = [
    "ops==3.*",
    "charmlibs-nginx",
]

[tool.uv.sources]
charmlibs-nginx = { path = "nginx" }
