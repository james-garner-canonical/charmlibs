# 0.8.0 - 10 December 2025

Release with Python 3.8 support. This is a 0.X release which does not guarantee extended support. Changes from the 1.0 release are purely `typing` related. The code is otherwise still identical to the `operator_libs_linux.v0.apt` library (patch version 19).
