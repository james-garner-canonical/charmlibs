(reference-testing)=
# Testing packages

Some libraries provide a separate testing package with helpers for writing state-transition tests against that library. A testing package is released in lockstep with the library it's for, and is installed through the library's `testing` extra rather than depended on directly.

Read more: {ref}`how-to-provide-data-for-charm-tests`

```{include} _testing-packages.md
```
