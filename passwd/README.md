# charmlibs.passwd

The `passwd` library.

To install, add `charmlibs-passwd` to your Python dependencies. Then in your Python code, import as:

```py
from charmlibs import passwd
```

See the [reference documentation](https://canonical.com/juju/docs/charmlibs/reference/charmlibs/passwd) for more.
